<h1>id: <%= @author.id %> のメッセージ詳細ページ</h1>

<p><%= @author.author_name %></p>

<ul>
  <% @author.books.each do |message| %>
    <li><%= link_to message.id, message %></li>
    <li><%= message.book_name %></li>
  <% end %>
</ul>
