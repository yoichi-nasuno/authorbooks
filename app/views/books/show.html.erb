<h1>id: <%= @book.id %> のメッセージ詳細ページ</h1>

<p><%= @book.book_name %></p>

<ul>
  <% @book.authors.each do |message| %>
    <li><%= link_to message.id, message %></li>
    <li><%= message.author_name %></li>
  <% end %>
</ul>